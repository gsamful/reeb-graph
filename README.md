# Reeb Graph

## Overview
A deterministic O(n * log(n))-time algorithm for computing Reeb graphs of
Morse functions defined on arbitrary simplicial complexes.  Written in Haskell!

Stay tuned for more information.